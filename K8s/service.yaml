apiVersion: v1
kind: Service
metadata:
  name: task-manager-service
  namespace: task-manager
spec:
  type: LoadBalancer
  selector:
    app: task-manager
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000