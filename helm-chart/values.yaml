replicaCount: 4

image:
  repository: anvesh3231/task-manager-app
  tag: latest

service:
  type: LoadBalancer
  port: 80
  targetPort: 3000

namespace: task-manager

mongo:
  image: mongo:7
  storageSize: 1Gi

secretName: app-secrets